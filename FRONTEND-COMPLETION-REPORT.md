# ResearchFlow MVP — Frontend Integration Report

**Date**: 2026-03-29
**Version**: v0.2.1-frontend

---

## Phase Summary

| Phase | Description | Status |
|-------|-------------|--------|
| 0 | API client base URL + dev user ID + missing methods | Done |
| 1 | Email briefing screen — translate/draft/extract-actions modal | Done |
| 2 | Capture screen API integration (already wired) | Done |
| 3 | Meeting screen API integration (already wired) | Done |
| 4 | Verify all screens connected to Railway backend | Done |
| 5 | Build verification + completion report | Done |

---

## Phase 0: API Client Updates

- **API_BASE** changed from `labflow-api.onrender.com` to `labflow-app-production.up.railway.app`
- **Dev user ID** unified from `dev-user-001` to `dev-user-seo` across all files
- **New API methods added** to `src/services/api-client.ts`:
  - `translateEmail()` — Gemini Flash translation
  - `createEmailDraft()` — Gmail drafts.create
  - `extractEmailActions()` — AI task/event extraction
  - `createCalendarEvent()` — Google Calendar events.insert
  - `brainChat()` — Mini-Brain chat
  - `listBrainChannels()`, `createBrainChannel()`, `getBrainChannelMessages()`, `deleteBrainChannel()`
  - `searchBrain()` — Lab Memory search
  - `getKnowledgeGraph()`, `getGraphNodeConnections()`
  - `getPaperAlerts()`, `getPaperAlertResults()`, `runPaperAlerts()`, `markPaperAlertRead()`
  - `getLabProfile()`, `getLabCompleteness()`
  - `getVoicePersonas()`, `createVoiceSession()`

## Phase 1: Email Briefing Screen

- Added email detail modal with 3 action buttons:
  - **Translate**: Gemini Flash Korean translation with result display
  - **Extract Actions**: AI task/event extraction with calendar add button
  - **Reply Draft**: Compose reply body, save to Gmail drafts
- Updated `EmailBriefingItem` type with `summary`, `messageId`, `threadId` fields

## Phase 2–3: Capture & Meeting (Already Integrated)

Both screens were already fully wired to the backend API:
- Capture: `useCapture` hook uses `createCapture`, `listCaptures`, `updateCapture`, `deleteCapture`, `clearCompletedCaptures`, `createVoiceCapture`
- Meeting: Uses `createMeeting`, `listMeetings`, `deleteMeeting` with full audio recording pipeline

## Phase 4: Railway URL Migration

Files updated with Railway URL + dev-user-seo:

| File | Changes |
|------|---------|
| `src/services/api-client.ts` | API_BASE + dev user ID |
| `src/providers/AuthProvider.tsx` | Default dev user ID |
| `src/hooks/useChat.ts` | Replaced mock responses with `brainChat()` API |
| `app/(tabs)/index.tsx` | Fixed broken `apiClient` import → `brainChat()` |
| `app/(tabs)/brain-chat.tsx` | Railway URL + dev user ID |
| `app/(tabs)/paper-alerts.tsx` | Railway URL + dev user ID |
| `app/(tabs)/knowledge-graph.tsx` | Railway URL + dev user ID |
| `app/(tabs)/onboarding.tsx` | Railway URL + dev user ID |
| `app/(tabs)/chatbot.tsx` | Railway URL |
| `server/src/middleware/auth.ts` | Default dev user ID |
| `server/src/routes/email.ts` | OAuth callback dev user ID |
| `web/src/lib/api.ts` | Railway URL + dev user ID |
| `web/src/app/settings/page.tsx` | Display info updated |

## Phase 5: Build Verification

- `npx tsc --noEmit` (server): 0 errors
- `npx tsc --noEmit` (mobile app, excluding web/): 0 errors
- `npm run build` (server): success

### TypeScript Fixes Applied
- Removed duplicate `summary`, `messageId`, `threadId` from `EmailBriefingItem`
- Added `tags` field to `Message` type
- Expanded `ChannelType` to include `'idea' | 'memo'`
- Made `ChatInput` recording props optional (for use in ChatScreen without recorder)
- Fixed `checkHealth()` return type

---

## Screen → API Connection Map

| Screen | Tab | API Endpoints |
|--------|-----|---------------|
| Mini-Brain | index | `POST /api/brain/chat` |
| AI Chat | brain-chat | `POST /api/brain/chat` |
| Capture | memo | `GET/POST/PATCH/DELETE /api/captures` |
| Paper Alerts | paper-alerts | `GET/POST /api/papers/alerts`, `POST /api/papers/alerts/run` |
| Knowledge Graph | knowledge-graph | `GET /api/graph` |
| Voice Chatbot | chatbot | `POST /api/voice/session` |
| Email Briefing | email | `GET /api/email/briefing`, `POST /api/email/translate,draft,extract-actions,calendar-event` |
| Meeting Notes | meeting | `POST /api/meetings` (audio upload), `GET /api/meetings`, `DELETE /api/meetings/:id` |
| Settings | onboarding | `POST /api/lab/onboarding`, `POST /api/lab/seed-paper` |

---

*Generated by Claude Opus 4.6 Auto-Mode Orchestrator*
